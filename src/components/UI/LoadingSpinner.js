import React from "react";
import classes from "./LoadingSpinner.module.css";

export default function LoadingSpinner({ className }) {
  return <div className={`${classes.loadingSpinner} className`}></div>;
}
